- Removing spaces between the values using TRIM()
- Removing NULL values
- Replace Values to data normalization or standardize
- Take the last Record using Row_Number()
- Using Substring() and Replace() to create new collumns, spliting it in different collumns
- Using ISNULL() to identify Nulls values and replace to 0 when necessary


